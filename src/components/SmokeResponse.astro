<div class="smoke-response" id="smoke-response" aria-live="polite">
  <p class="smoke-text"></p>
</div>

<script>
  import "./smokeResponse.js"
</script>

<style>
  .smoke-response {
    position: fixed;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 10vh 8vw;
    pointer-events: none;
    z-index: 40;
  }

  .smoke-text {
    margin: 0;
    max-width: 640px;
    text-align: center;
    font-family: var(--secondary-font);
    color: var(--text);
    font-size: clamp(1.1rem, 2.4vw, 1.6rem);
    line-height: 1.6;
    opacity: 0;
  }

  .smoke-text :global(.word) {
    display: inline-block;
    opacity: 0;
    will-change: transform, filter, opacity;
  }
</style>
